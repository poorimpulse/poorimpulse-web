extends ./_layout.jade

block content
  .episodeList
    each episode in episodes
      .episode.captured(id='episode#{episode.guid}')
        .guid
          = episode.guid
          | .&nbsp;
        .description
          a(href=episode.permalink)
            = episode.title
          .narrator
            | narrated by&nbsp;
            +person(episode.narrated_by)
          p.summary
            = episode.summary

          p.contributors
            | Story by:&nbsp;
            - each contributor, index in episode.story_by
              - if(index !== 0)
                | ,&nbsp;
              +person(contributor)
          +audio(episode)
